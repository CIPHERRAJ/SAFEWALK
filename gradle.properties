# Enable AndroidX
android.useAndroidX=true
# Use false because we don't have legacy support-v4 libraries to transform
android.enableJetifier=false

# Use standard R class for faster builds and fewer resource conflicts
android.nonTransitiveRClass=true

# Default JVM options
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
